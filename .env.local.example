# LOCAL DEVELOPMENT ONLY (.env.local)
# NOTE: For Cloudflare Pages deployment, use wrangler.toml and wrangler secrets

# Ably API Key (if using locally)
# Get your API key from https://ably.com/accounts
ABLY_API_KEY=your_ably_api_key_here

# Cloudflare Workers AI - No API key required!
# Workers AI uses the AI binding configured in wrangler.toml
# The AI binding is automatically available when deployed to Cloudflare Pages
# For local development, use: wrangler pages dev
